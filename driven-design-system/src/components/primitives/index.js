/**
 * Driven Properties Design System — Primitives barrel export
 *
 * These are the lowest-level building blocks of the system.
 * They are composable, fully prop-driven and have zero business logic.
 *
 * Import from here:
 *   import { Button, Badge, Input, Tabs, Card } from '@/components/primitives'
 */

export { default as Button } from './Button';
export { default as Badge  } from './Badge';
export { default as Input  } from './Input';
export { default as Tabs   } from './Tabs';
export { default as Card   } from './Card';
